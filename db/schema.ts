import { integer, sqliteTable, text, uniqueIndex } from "drizzle-orm/sqlite-core";
export const users = sqliteTable("users", { id:text("id").primaryKey(), externalId:text("external_id").notNull().unique(), email:text("email").notNull().unique(), name:text("name").notNull(), role:text("role",{enum:["teacher","student","admin"]}).notNull(), active:integer("active",{mode:"boolean"}).notNull().default(true) });
export const courses = sqliteTable("courses", { id:text("id").primaryKey(), code:text("code").notNull().unique(), name:text("name").notNull(), teacherId:text("teacher_id").notNull().references(()=>users.id), room:text("room") });
export const enrollments = sqliteTable("enrollments", { id:text("id").primaryKey(), courseId:text("course_id").notNull().references(()=>courses.id), studentId:text("student_id").notNull().references(()=>users.id) }, t=>[uniqueIndex("idx_enrollment_course_student").on(t.courseId,t.studentId)]);
export const sessions = sqliteTable("attendance_sessions", { id:text("id").primaryKey(), courseId:text("course_id").notNull().references(()=>courses.id), openedBy:text("opened_by").notNull().references(()=>users.id), openedAt:integer("opened_at",{mode:"timestamp_ms"}).notNull(), closedAt:integer("closed_at",{mode:"timestamp_ms"}), cameraStatus:text("camera_status").notNull().default("offline"), cameraTokenHash:text("camera_token_hash").notNull() });
export const attendance = sqliteTable("attendance", { id:text("id").primaryKey(), sessionId:text("session_id").notNull().references(()=>sessions.id), studentId:text("student_id").notNull().references(()=>users.id), recognizedAt:integer("recognized_at",{mode:"timestamp_ms"}).notNull(), confidence:integer("confidence").notNull(), source:text("source").notNull().default("yolo") }, t=>[uniqueIndex("idx_attendance_session_student").on(t.sessionId,t.studentId)]);
export const notifications=sqliteTable("notifications",{id:text("id").primaryKey(),userId:text("user_id").notNull().references(()=>users.id),title:text("title").notNull(),message:text("message").notNull(),createdAt:integer("created_at",{mode:"timestamp_ms"}).notNull(),readAt:integer("read_at",{mode:"timestamp_ms"})});
